<h1>Lista de categorias:</h1>
<hr>
<div>
    <a href="/admin/categorias/add"> <button class="btn btn-success">Nova categoria</buton> </a>
</div>

{{#each categorias }}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{nome}}</h4>
            <small>{{slug}}</small>
            <h4>testendo nao esta mostrando valores</h4>
        </div>
    </div>
 
{{else}}

{{/each}}