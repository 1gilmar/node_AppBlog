<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao blog</h1>
  <p class="lead">Este e um Blog simples desenvolvido com NodeJS + Express + MongoDB.</p>
  <hr class="my-4">
  <p>Aqui sera mostrado o que a pessoa quiser mostrar.</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Criar conta</a>
</div>

<h2>Postagens:</h2>

{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{titulo}}</h4>
            <p>{{descricao}}</p>
            <a href="/postagem/{{slug}}"><button class="btn btn-info">ler mais...</button></a>
            <br>
            <hr>
            <small>{{categoria.nome}}</small>
            <br>
            <small>Data de publicação: {{dataCriacao}} </small>
        </div>
    </div>
{{else}}

{{/each}}